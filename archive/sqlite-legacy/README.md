# SQLite Legacy Archive

These files are archived from earlier SQLite-based development.

They are not used by the current backend runtime.

Current official database:
- MySQL
- Configured through `backend/.env` with `DATABASE_URL`

Archived here:
- `app.db`
- `database.db`
- `rideshare.db`
- `clear_rides_db.py`
