# Backend Scripts

Operational backend scripts live here.

Use these for:
- schema and migration helpers
- seeded data setup
- intentional maintenance tasks

These scripts assume MySQL is the active backend database and read configuration from `backend/.env`.
